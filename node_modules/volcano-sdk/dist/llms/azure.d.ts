import type { LLMHandle } from "./types.js";
type AzureAIClient = {
    createResponse: (params: any) => Promise<any>;
};
export type AzureOptions = {
    max_output_tokens?: number;
    seed?: number;
};
export type AzureConfig = {
    model: string;
    endpoint: string;
    apiVersion?: string;
    apiKey?: string;
    accessToken?: string;
    client?: AzureAIClient;
    options?: AzureOptions;
};
export declare function llmAzure(cfg: AzureConfig): LLMHandle;
export {};
